Basic graphing calculator. Implements Marching Squares to graph functions.

The ability to move around in each graph is in the works, but I wanted to make a grapher that can graph coordinate transformations first and foremost, so emphasis was on shapes and curves.
